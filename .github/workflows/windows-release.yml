name: Windows desktop release (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Windows releases are paused while development focuses on macOS."
