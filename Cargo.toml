[package]
name = "codex-roster"
version = "0.2.47"
edition = "2024"
description = "Account switching for Codex CLI and IDE"
license = "MIT"
repository = "https://github.com/anlvdt/codex-roster"
homepage = "https://github.com/anlvdt/codex-roster"
readme = "README.md"
keywords = ["codex", "cli", "ide", "accounts"]
categories = ["command-line-utilities"]

[dependencies]
anyhow = "1.0.102"
age = "0.11.2"
base64 = "0.22.1"
clap = { version = "4.6.0", features = ["derive"] }
console = "0.16.3"
dialoguer = "0.12.0"
directories = "6.0.0"
flate2 = "1.1.5"
fs2 = "0.4.3"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
sha2 = "0.10.9"
sysinfo = "0.38.4"
time = { version = "0.3.47", features = ["formatting", "local-offset", "parsing", "serde"] }
ureq = { version = "3.1.2", features = ["json"] }
uuid = { version = "1.23.0", features = ["serde", "v4"] }

[target.'cfg(target_os = "windows")'.dependencies]
image = { version = "0.25.10", default-features = false, features = ["png", "ico"] }
keyring = { version = "3.6.3", features = ["windows-native"] }
tray-icon = { version = "0.22.1", default-features = false }
windows-sys = { version = "0.61.2", features = ["Win32_Foundation", "Win32_System_Console", "Win32_UI_WindowsAndMessaging"] }
winit = { version = "0.30.13", default-features = false }

[target.'cfg(target_os = "windows")'.build-dependencies]
winresource = "0.1.31"

[target.'cfg(target_os = "macos")'.dependencies]
keyring = { version = "3.6.3", features = ["apple-native"] }

[target.'cfg(target_os = "linux")'.dependencies]
keyring = { version = "3.6.3", features = ["sync-secret-service", "vendored"] }

[dev-dependencies]
tempfile = "3.27.0"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
